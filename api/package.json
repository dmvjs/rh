{
  "name": "rh-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply rh",
    "db:migrate:local": "wrangler d1 migrations apply rh --local",
    "db:seed-admin": "node scripts/seed-admin.js"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
